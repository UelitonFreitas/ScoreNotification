package com.notification.score.scorenotification.imageprocessor

import android.graphics.Bitmap

class ImageProcessorImpl: ImageProcessor {
    override fun processImage(image: Bitmap): Bitmap {
        return image
    }
}